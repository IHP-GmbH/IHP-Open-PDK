# The bulks are tied directly to VSS/VDD and the schematic has no ntap1/ptap1
# devices, so tap extraction must be disabled for this case to match. Deep mode
# keeps the dummy devices embedded in the NMOS/PMOS cells (flat mode merges them).
test_ntap_ptap_ext:
  "--run_mode": deep
  "--disable_tap_extraction": true
