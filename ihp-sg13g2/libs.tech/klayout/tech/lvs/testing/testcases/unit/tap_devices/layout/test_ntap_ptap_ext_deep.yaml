# Same layout as test_ntap_ptap_ext, but run in deep mode with tap extraction
# ENABLED (the default). The ntap1/ptap1 guard rings inside the nmos/pmos subcells
# are then extracted cell-locally through the cheat-based guard-ring extraction,
# so this case covers the hierarchical (deep-mode) tap extraction path.
test_ntap_ptap_ext_deep:
  "--run_mode": deep
