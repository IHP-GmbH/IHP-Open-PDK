Xyce/ADMS adaptions:
Use IHP specific code for AD, AS, PD, PS calculation.
Remove $mfactor from OP output variables calculation
Omit TRISE instance parameter for standalone juncap200 because parameter scope problem
Use DTA and TRISE as instance parameter and model parameter. Instance parameter will 
overwrite model parameter.
Temperature initialization code must be shifted and pseudo event blocks needed.

Introduce SOAcheck extension to issue warnings at overvoltage for certain branches.

Code version 103.8.2 downloaded from http://www.cea.fr/cea-tech/leti/pspsupport.
